const Initial_State = {
  token: null,
  firstName: "",
  lastName: "",
  errorMessage: "",
  assistants: [],
};

const userReducer = (state = Initial_State, action) => {
  return state;
};

export default userReducer;
